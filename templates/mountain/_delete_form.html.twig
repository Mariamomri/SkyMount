<form method="post" action="{{ path('app_mountain_delete', {'id': mountain.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mountain.id) }}">
    <button class="btn">Delete</button>
</form>
